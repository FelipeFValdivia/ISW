
<div class="row">
  <div class="col s12 m6 offset-m3">
    <div class="card create-teacher-card">
      <div class="card-content black-text">
        <div class="row">
          <span class="card-title">Crear nuevo contentido para <%=@course.name%></span>
  				<%= form_for @content, url: content_create_course_path(course_id: @course.id), method: :post do |f| %>
  					<%= f.text_field :name, placeholder: "Nombre del contenido", required: true  %>
  					<%= f.text_area :description, placeholder: "Descripción ", required: true  %>
            <br>
            <br>
            <%= content_tag :button,  :type => :submit, :class => 'col s8 offset-s2 waves-effect waves-ripple btn' do %>
               Crear
            <% end %>						
  				<% end -%>
        </div>  
      </div>
    </div>
  </div>
</div>